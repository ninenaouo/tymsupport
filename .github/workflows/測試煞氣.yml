
name: test-synology-chat

on:
  workflow_dispatch:   # 手動執行用

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Debug call
        run: |
          curl -sS -i -X POST "$SYNO_CHAT_URL" \
          -H "Content-Type: application/json" \
          -d '{"text":"🧪 test"}' \
          -w "\nHTTP_CODE:%{http_code}\n"
    env:
      SYNO_CHAT_URL: ${{ secrets.SHACHI_TOKEN }}
